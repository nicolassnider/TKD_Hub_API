NEXT_PUBLIC_API_BASE_URL=sample_url
# Public key for MercadoPago client-side widgets (keep secret keys on server)
NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY=your_public_key_here

# Backend endpoint used to create MercadoPago preferences (server stores secrets)
# Example: http://localhost:5000
# NEXT_PUBLIC_API_BASE_URL should point to your WebAPI base URL
